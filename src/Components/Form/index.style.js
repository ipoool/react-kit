import styled from 'styled-components';

const Form = ComponentName => styled(ComponentName)`
  .ant-form-item-label {
    label {
      font-weight: 600;
      color: #898d97;
    }
  }
`;

export default Form;
